class Creator {
  ArrayList<RShape> shapes = new ArrayList<RShape>();
  Machine machine;
  Settings settings;
  Creator(Machine t_machine, Settings t_settings) {
    machine = t_machine;
    settings = t_settings;
  }
}
